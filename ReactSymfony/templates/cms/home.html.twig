{% extends 'base.html.twig' %}

{% block title %}{{controller_name}}{% endblock %}

{% block body %}

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <div {{ react_component('App', { 'username': app.user.username }) }}></div>
    {% else %}
        <div {{ react_component('App', { 'username': "" }) }}></div>
    {% endif %}

{% endblock %}
